package gravity_api

const (
	baseUrl = "https://gravity.creativeappnow.com"
)

const (
	userUrl   = baseUrl + "/gravity/user"
	commonUrl = baseUrl + "/gravity/common"
	pushUrl   = baseUrl + "/gravity/push"
)
